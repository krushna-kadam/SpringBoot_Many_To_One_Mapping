# SpringBoot_Many_To_One_Mapping
Many-to-One Mapping in Spring Boot Description:  In a Many-to-One relationship, multiple child entities are associated with a single parent entity. For example, many students can belong to one department. Spring Boot with JPA/Hibernate makes it easy to map such relationships using annotations like @ManyToOne and @JoinColumn.
